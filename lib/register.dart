import 'package:flutter/material.dart';

void main(){
  return runApp(Register());
}

class Register extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Material(
      title: 'Register page',

      );
  
  }

}